import React from 'react';
import CouponContainer from '../../containers/main/CouponContainer';

const CouponPage = ({match}) => {
    return <CouponContainer match={match} />;
};

export default CouponPage;
